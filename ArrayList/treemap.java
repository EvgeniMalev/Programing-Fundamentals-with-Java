import java.util.*;
import java.util.Decimal;

public class Main {
       public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        String input = scanner.nextLine();

        List<Double> degrees = input.split(" ");

        TreeSet<String, List<Double>> = String => List<Double>>;

        for (String double : numbers) {

        }

        for (TreeMap.Entry<String Double> entry : countMap.entrySet()) {
            System.out.println(df.format(entry.getKey() + " " + entry.getValue());
        }

        scanner.close();
     }
}
